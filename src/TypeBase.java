

/*
 * Represent a type node inside the AST.
*/

public abstract class TypeBase extends Type {
    /**
     * Constructor
     */
    public TypeBase(String typeName) {
        super(typeName);
    }
}
